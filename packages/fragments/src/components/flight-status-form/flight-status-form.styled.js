import styled from 'styled-components';
import { StyledFieldContainer } from '../styled/app.styled';

export const StyledFieldContainerExtended = styled(StyledFieldContainer)`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
`;

export const StyledSeparationText = styled.div`
  font-size: 15px;
  font-weight: 600;
`;
